# 项目协作规则

- 单个文件大小不能超过 800 行。
- 修改或新增文件时需要先关注文件行数；如果文件超过 800 行，必须按职责拆分为多个文件后再继续维护。
- 演示后台的每个模块均需要有详细的 Markdown 需求说明文档：先维护一份总说明，再为每个模块维护对应的功能需求说明文档。
- 每次更新页面功能后，必须同步更新对应模块的 Markdown 文档，确保页面实现与需求说明保持一致。
